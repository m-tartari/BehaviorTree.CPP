services:
  behaviortree:
    image: behaviortree_cpp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1667:1667"
    # volumes:
    #   - .:/workspaces/BehaviorTree.CPP
